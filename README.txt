To compile on terminal type
  make all

To delete executables and object file type
  make clean

Executed mainly on windows machine
  test_chain.exe < test_input_file.txt

(i). All parts of the assignment were executed
	-Implement the “big five”. Add the output stream << operator.  
	-Overload the + and [] operators for our Chain class.

(ii). There are no known bugs in the program
 
(iii). The input file is test_input_file.txt and we were given an output file so we could track our progress and results,
       however, it is not needed for this project.  
  
